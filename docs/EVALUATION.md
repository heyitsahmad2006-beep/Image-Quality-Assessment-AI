# Evaluation Guide

Run the complete evaluation suite on the held-out test split:
```bash
python -m ml.evaluation.evaluate
```

This generates:
- `artifacts/metrics.json`
- `artifacts/evaluation_report.md`
